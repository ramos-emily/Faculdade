# Questão 1
# Dado um conjunto de inteiros nums e um inteiro alvo, retorne os índices dos dois números de forma que a soma deles seja o alvo. Você pode assumir que cada entrada terá exatamente uma solução e não pode usar o mesmo elemento duas vezes. Deverá ser construída uma função (chamada f) que receberá os dois argumentos nums e alvo.

# Exemplo 1
# Entrada: nums = [2, 7, 11, 15], alvo = 9
# Saída: (0, 1)
# Exemplo 2
# Entrada: nums = [3, 2, 4], alvo = 6
# Saída: (1, 2)
# Exemplo 3
# Entrada: nums = [3, 3], alvo = 6
# Saída: (0, 1)

nums = [2, 7, 11, 15]
alvo = 9
print(0, 1)